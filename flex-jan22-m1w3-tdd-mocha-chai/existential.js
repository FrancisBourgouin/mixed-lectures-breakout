// Looking at a solution makes sense, but getting to it is complicated ++++

// Dunning Kruger Effect

// .indexOf -1
// .length === 0

// Input (What goes in)

// Output (What comes out)

const arr1 = [1, 2, 3];
const arr2 = [1, 2, 3];
const d2 = [1, 2, 3, 4];

// First, are they the same length?
// Is the first element in the first array the same as the second one
// Is the second element in the first array the same as the second one
// Is the third element in the first array the same as the second one

arr1[0] === arr2[0];
arr1[1] === arr2[1];
arr1[2] === arr2[2];

arr1[index] === arr2[index];

for (const index in arr1) {
}

for (let index = 0; index < arr1.length; index++) {}
