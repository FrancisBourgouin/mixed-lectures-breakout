<p style="color: green"><%= notice %></p>

<%= "You are this user!" if @user.id == current_user.id %>

<%= render @user %>

<div>
  <%= link_to "Edit this user", edit_user_path(@user) %> |

  <%= button_to "Destroy this user", @user, method: :delete %>
</div>
